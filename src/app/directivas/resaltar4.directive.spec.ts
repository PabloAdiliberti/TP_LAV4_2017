import { Resaltar4Directive } from './resaltar4.directive';

describe('Resaltar4Directive', () => {
  it('should create an instance', () => {
    const directive = new Resaltar4Directive();
    expect(directive).toBeTruthy();
  });
});
